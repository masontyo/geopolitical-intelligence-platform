{
  "name": "my-app-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --coverage --watchAll=false",
    "test:watch": "jest --watch",
    "test:api": "jest --testPathPattern=api --coverage --watchAll=false",
    "test:integration": "jest --testPathPattern=integration --coverage --watchAll=false",
    "test:unit": "jest --testPathPattern=unit --coverage --watchAll=false",
    "seed": "node scripts/seedDatabase.js",
    "seed:reset": "node scripts/seedDatabase.js --reset"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "mongoose": "^8.17.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^5.2.2",
    "html-webpack-plugin": "^5.6.3",
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "supertest": "^6.3.0"
  }
}
